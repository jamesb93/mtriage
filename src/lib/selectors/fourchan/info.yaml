desc: Allows you to scrape text and media from 4chan
args:
  - name: board
    desc: Identifier for a specific board to scrape. If not specified all boards are scraped.
    required: true
    input: string
  - name: max_pages
    desc: Number of total pages to scrape.
    required: true
    input: integer
